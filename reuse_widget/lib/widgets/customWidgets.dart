import 'package:flutter/material.dart';

class CustomButton extends StatelessWidget {
  final String inputText;
  final double height;

  const CustomButton({required this.inputText}); //The way to Custom

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
